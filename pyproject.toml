[project]
name = "excel-pipeline"
version = "0.1.0"
description = "Standalone Excel transformation pipeline driven by Claude."
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.30",
    "anthropic>=0.40",
    "openpyxl>=3.1",
    "pandas>=2.0",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["excel_pipeline"]
